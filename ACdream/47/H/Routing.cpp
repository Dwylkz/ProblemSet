#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <algorithm>
#include <cstring>
#include <string>
using namespace std;
#define Rep(c, a, b) for (int c = a; c < b; c++)
#define Nre(c, a, b) for (int c = a; c > b; c--)
#define MAXN (14)
#define SIGM (512)
typedef int AI[MAXN];

int n;

int main()
{
    #if 1
        freopen("test.txt", "rt", stdin);
    #endif

    while (scanf("%d", &n), n)
    {

    }

    return 0;
}
