#include <cstdio>
#include <cstdlib>
#include <iostream>
using namespace std;
#define Rep(c, a, b) for (int c = a; c < b; c++)
#define MAXN (2000)

char A[MAXN], B[MAXN];

int main()
{
    #if 1
        freopen("test.txt", "rt", stdin);
    #endif
    int T;

    scanf("%d", &T);
    Rep(C, 0, T)
    {
        scanf("%d%d", A, B);
    }

    return 0;
}
