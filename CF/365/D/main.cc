#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>
using namespace std;
const int N = 50+10;
const int C = 1e4+10;
const int M = N*C;

int n, d, dp[N][M];

int main() {
#if 1
  freopen("input.in", "r", stdin);
#endif
  for ( ; ~scanf("%d%d", &n, &d); ) {
  }
  return 0;
}
