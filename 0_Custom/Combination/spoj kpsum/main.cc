#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>
using namespace std;
typedef long long LL;

LL n;

LL askf(int f, int g) {
  LL rv = 0;
  if (g&1) {
  } else {
  }
  return 0;
}
LL askp(LL p, int f) {
  return 0;
}
LL askn(LL n) {
  return 0;
}

int main() {
#if 1
  freopen("input.in", "r", stdin);
#endif
  for ( ; ~scanf("%lld", &n); ) {
    printf("%lld\n", askn(n));
  }
  return 0;
}
